##React Player Vs. Player

## Available Scripts

In the project directory, you can run:

To install all dependencies

### `npm -i` || `yarn install`

To Start the app

### `npm start` \\ `yarn start`
